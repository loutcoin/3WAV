// SPDX-License-Identifier: MIT
pragma solidity ^0.8.24;

// Deprecated because can be handled ENTIRELY by front-end

library VersionStemStorage {
    /* bytes32 constant STORAGE_SLOT = keccak256("Version.Stem.Map.Storage");

    struct VersionStemMap {
        mapping(bytes32 hashId => mapping(uint8 versionIndex => bytes32 versionHash)) s_contentVersion;
        //mapping(bytes32 hashId => uint8 stemId) internal s_stemTracks;
    }


    function versionStructStorage()
        internal
        pure
        returns (VersionStemMap storage VersionStemMapStruct)
    {
        bytes32 _storageSlot = STORAGE_SLOT;
        assembly {
            VersionStemMapStruct.slot := _storageSlot
        }
    } */
}
